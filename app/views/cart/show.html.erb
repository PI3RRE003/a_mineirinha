<div 
  class="fixed inset-0 -z-10 bg-repeat"
  style="background-image: url('<%= asset_path('Background.png') %>'); background-size: 300px;"
>
</div>

<%# 
   AJUSTE RESPONSIVO: 
   - Reduzi o padding vertical no mobile (py-6) e mantive maior no desktop (md:py-12).
%>
<div class="w-full py-6 md:py-12 px-4 sm:px-6 lg:px-8 font-sans">
  
  <div class="max-w-4xl mx-auto">
    
    <%# Botão de Voltar %>
    <div class="mb-6">
      <%= link_to root_path, class: "inline-flex items-center text-[#32491E] bg-amber-100 rounded-full p-2 pr-4 hover:text-orange-600 font-bold transition shadow-sm" do %>
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-5 h-5 mr-2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5 3 12m0 0 7.5-7.5M3 12h18" />
        </svg>
        Continuar Comprando
      <% end %>
    </div>

    <%# 
       CARD DO CARRINHO 
       - rounded-3xl no mobile (menos agressivo) e rounded-[40px] no desktop.
    %>
    <div class="bg-amber-100 rounded-3xl md:rounded-[40px] shadow-2xl border-4 border-white overflow-hidden flex flex-col min-h-[400px] md:min-h-[500px]">
      
      <%# Cabeçalho do Card %>
      <div class="px-6 py-5 md:px-8 md:py-6 border-b border-orange-200/50 bg-amber-50/80 flex flex-col sm:flex-row justify-between items-start sm:items-center gap-2">
        <h1 class="text-2xl md:text-3xl font-bold text-[#32491E] flex items-center gap-2">
          🛒 Seu Carrinho
        </h1>
        <span class="bg-orange-100 text-orange-800 text-xs font-bold px-3 py-1 rounded-full self-start sm:self-auto">
          <%= @order.order_items.count %> itens
        </span>
      </div>

      <%# Lista de Itens %>
      <div class="flex-1 p-4 md:p-8">
        <% if @order.order_items.any? %>
          
          <div class="space-y-4">
            <% @order.order_items.each do |item| %>
              <%# 
                 ITEM DO CARRINHO 
                 - Mantive flex-row para imagem ficar ao lado do texto.
                 - Ajustei tamanhos de fonte e imagem para mobile.
              %>
              <div class="bg-white p-3 md:p-4 rounded-3xl shadow-sm border border-stone-100 flex items-center gap-3 md:gap-4 transition hover:shadow-md">
                
                <div class="h-16 w-16 md:h-20 md:w-20 bg-stone-100 rounded-2xl overflow-hidden flex-shrink-0">
                   <% if item.product.imagem.attached? %>
                      <%= image_tag item.product.imagem.variant(resize_to_limit: [150, 150]), class: "h-full w-full object-cover" %>
                   <% else %>
                      <img src="https://placehold.co/150x150/FFF7ED/CA8A04?text=Queijo" alt="<%= item.product.nome %>" class="h-full w-full object-cover">
                   <% end %>
                </div>

                <div class="flex-1 min-w-0"> <%# min-w-0 evita que texto quebre o layout %>
                  <h3 class="font-bold text-[#32491E] text-base md:text-lg leading-tight truncate"><%= item.product.nome %></h3>
                  <p class="text-stone-500 text-xs md:text-sm"><%= number_to_currency(item.preco_unitario) %> un.</p>
                </div>

                <div class="text-right whitespace-nowrap">
                  <div class="text-[10px] md:text-xs text-stone-400 font-bold uppercase tracking-wider">Qtd: <%= item.quantidade %></div>
                  <div class="text-sm md:text-lg font-bold text-orange-600 mt-0.5 md:mt-1">
                    <%= number_to_currency(item.preco_unitario * item.quantidade) %>
                  </div>
                </div>

                <div class="pl-2 md:pl-4 border-l border-stone-100">
                  <%= link_to remove_from_cart_path(item.id), data: { turbo_method: :delete }, class: "text-stone-300 hover:text-red-500 transition p-2 block", title: "Remover item" do %>
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-5 h-5 md:w-6 md:h-6">
                      <path stroke-linecap="round" stroke-linejoin="round" d="m14.74 9-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 0 1-2.244 2.077H8.084a2.25 2.25 0 0 1-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 0 0-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 0 1 3.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 0 0-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 0 0-7.5 0" />
                    </svg>
                  <% end %>
                </div>

              </div>
            <% end %>
          </div>

        <% else %>
          <%# Carrinho Vazio %>
          <div class="h-full flex flex-col items-center justify-center text-center py-12 md:py-20">
            <div class="text-5xl md:text-6xl mb-4 bg-white p-6 rounded-full shadow-sm">🥣</div>
            <h2 class="text-xl md:text-2xl font-bold text-stone-700">Seu cesto está vazio!</h2>
            <p class="text-stone-500 mt-2 max-w-xs mx-auto text-sm md:text-base">Que tal encher de pão de queijo quentinho?</p>
            <div class="mt-8">
              <%= link_to root_path, class: "bg-[#32491E] text-white px-8 py-3 rounded-2xl font-bold shadow-lg shadow-green-900/20 hover:bg-green-900 hover:-translate-y-1 transition transform inline-block" do %>
                Ir para o Cardápio
              <% end %>
            </div>
          </div>
        <% end %>
      </div>

      <%# Área de Checkout %>
      <% if @order.order_items.any? %>
        <div class="bg-white p-6 md:p-8 border-t border-stone-100">
          
          <%# 
             FORMULÁRIO UNIFICADO 
             - flex-col no mobile (pagamento em cima, resumo embaixo).
             - md:flex-row no desktop (lado a lado).
          %>
          <%= form_with url: finalizar_pedido_path, method: :post, local: true, class: "flex flex-col md:flex-row gap-8" do |f| %>
            
            <%# Coluna Pagamento (Esquerda/Topo) %>
            <div class="flex-1 space-y-4">
              <h3 class="font-bold text-[#32491E] text-lg mb-2">Como deseja pagar?</h3>
              
              <div class="grid grid-cols-1 sm:grid-cols-3 gap-3">
                <label class="cursor-pointer group relative">
                  <input type="radio" name="tipo_pagamento" value="Pix" class="peer sr-only" required>
                  <div class="h-full p-4 rounded-xl border-2 border-stone-100 bg-stone-50 peer-checked:border-green-500 peer-checked:bg-green-50 flex flex-row sm:flex-col items-center justify-start sm:justify-center gap-3 sm:gap-2 hover:border-orange-200 transition-all">
                    <span class="text-xl md:text-2xl">💠</span>
                    <span class="font-bold text-sm text-stone-600 peer-checked:text-green-800">Pix</span>
                  </div>
                </label>

                <label class="cursor-pointer group relative">
                  <input type="radio" name="tipo_pagamento" value="Cartão de Crédito" class="peer sr-only">
                  <div class="h-full p-4 rounded-xl border-2 border-stone-100 bg-stone-50 peer-checked:border-orange-500 peer-checked:bg-orange-50 flex flex-row sm:flex-col items-center justify-start sm:justify-center gap-3 sm:gap-2 hover:border-orange-200 transition-all">
                    <span class="text-xl md:text-2xl">💳</span>
                    <span class="font-bold text-sm text-stone-600 peer-checked:text-orange-800">Cartão</span>
                  </div>
                </label>

                <label class="cursor-pointer group relative">
                  <input type="radio" name="tipo_pagamento" value="Dinheiro / Espécie" class="peer sr-only">
                  <div class="h-full p-4 rounded-xl border-2 border-stone-100 bg-stone-50 peer-checked:border-stone-500 peer-checked:bg-stone-200 flex flex-row sm:flex-col items-center justify-start sm:justify-center gap-3 sm:gap-2 hover:border-orange-200 transition-all">
                    <span class="text-xl md:text-2xl">💵</span>
                    <span class="font-bold text-sm text-stone-600 peer-checked:text-stone-900">Dinheiro</span>
                  </div>
                </label>
              </div>
            </div>

            <%# Coluna Resumo (Direita/Baixo) %>
            <div class="w-full md:w-80 flex-shrink-0 flex flex-col gap-6">
              
              <div class="bg-amber-50 rounded-2xl p-4 border border-amber-100">
                <h3 class="text-xs font-bold text-[#32491E] uppercase tracking-wider mb-2">Entregar em:</h3>
                <% if current_user.endereco.present? %>
                  <p class="text-stone-700 font-medium text-sm leading-relaxed truncate">
                    <%= current_user.respond_to?(:endereco_completo) ? current_user.endereco_completo : current_user.endereco %>
                  </p>
                  <%= link_to "Alterar", perfil_usuario_path, class: "text-xs text-orange-600 font-bold hover:underline mt-2 inline-block" %>
                <% else %>
                  <div class="flex items-center gap-2 text-red-500 mb-1">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd" /></svg>
                    <span class="font-bold text-xs">Falta o endereço!</span>
                  </div>
                  <%= link_to "Cadastrar Agora", perfil_usuario_path, class: "text-sm text-blue-600 underline font-medium" %>
                <% end %>
              </div>

              <div class="flex items-center justify-between border-t border-dashed border-stone-200 pt-4">
                <span class="text-stone-500 font-medium">Total</span>
                <span class="text-3xl font-extrabold text-[#32491E]">
                  <%= number_to_currency(@order.total) %>
                </span>
              </div>

              <button type="submit" class="w-full py-4 rounded-2xl text-lg font-bold text-white bg-[#32491E] hover:bg-green-900 shadow-xl transition transform hover:-translate-y-1 flex justify-center items-center gap-2 cursor-pointer disabled:bg-stone-300 disabled:cursor-not-allowed disabled:shadow-none" <%= 'disabled' if current_user.endereco.blank? %>>
                <span>Confirmar Pedido</span>
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor" class="w-6 h-6"><path stroke-linecap="round" stroke-linejoin="round" d="M17.25 8.25 21 12m0 0-3.75 3.75M21 12H3" /></svg>
              </button>
            </div>
          <% end %> <%# Fim do Form %>

        </div>
      <% end %>

    </div>

  </div>

  <%# --- HISTÓRICO RESPONSIVO --- %>
  <% if @historico.any? %>
    <div class="my-12 border-t-2 border-dashed border-stone-200"></div>

    <div class="max-w-4xl mx-auto pb-12">
      <h2 class="text-2xl font-bold bg-amber-100 rounded-lg text-[#32491E] mb-6 flex items-center gap-2 px-2 md:px-0">
        📜 Meus Pedidos Anteriores
      </h2>

      <div class="grid grid-cols-1 md:grid-cols-2 gap-4 md:gap-6">
        <% @historico.each do |pedido| %>
          
          <div class="bg-white rounded-2xl p-5 shadow-sm border border-stone-200 hover:shadow-md transition">
            
            <div class="flex justify-between items-start mb-4">
              <div>
                <span class="text-xs font-bold text-stone-400 uppercase">Pedido #<%= pedido.id %></span>
                <div class="text-sm text-stone-500"><%= pedido.updated_at.strftime("%d/%m/%Y às %H:%M") %></div>
              </div>

              <% 
                cor_status = case pedido.status
                  when 'Recebido' then 'bg-orange-100 text-orange-700 border-orange-200'
                  when 'Concluído' then 'bg-green-100 text-green-700 border-green-200'
                  when 'Cancelado' then 'bg-red-50 text-red-600 border-red-100 line-through'
                  else 'bg-stone-100 text-stone-600 border-stone-200'
                end
              %>
              <span class="px-3 py-1 rounded-full text-xs font-bold border <%= cor_status %>">
                <%= pedido.status %>
              </span>
            </div>

            <div class="bg-stone-50 rounded-lg p-3 mb-4 text-sm">
              <ul class="space-y-1">
                <% pedido.order_items.each do |item| %>
                  <li class="flex justify-between text-stone-600">
                    <span><%= item.quantidade %>x <%= item.product.nome %></span>
                  </li>
                <% end %>
              </ul>
            </div>

            <div class="flex justify-between items-center pt-2 border-t border-stone-100">
              <div class="flex items-center gap-1.5 text-xs font-bold text-stone-500">
                <% if pedido.tipo_pagamento == 'Pix' %> 💠 Pix
                <% elsif pedido.tipo_pagamento == 'Cartão de Crédito' %> 💳 Cartão
                <% else %> 💵 Dinheiro
                <% end %>
              </div>
              <div class="font-bold text-[#32491E] text-lg">
                <%= number_to_currency(pedido.total) %>
              </div>
            </div>

          </div>
        <% end %>
      </div>
    </div>
  <% end %>

</div>